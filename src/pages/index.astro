---
import LayoutIndex from '../layouts/LayoutIndex.astro';
import ButtonLetter from '../components/ButtonLetter.astro';
import ContainerPresentation from '../layouts/ContainerPresentation.astro';
---

<LayoutIndex title="LoloBelo & Pizzas | Menu LoloBelo | Pizzeria">
	<main>
		<ContainerPresentation>
			<div>
				<h1>
					Bienvenido a
				</h1>
				<img src="./logoLoloBelo.webp" alt="Logo LoloBelo">
			</div>
			<ButtonLetter />
		</ContainerPresentation>
	</main>
</LayoutIndex>

<style>
	:root {
    --color-primary: #ea3333
  }
	main {
		width: 100vw;
		height: 100vh;

		display: flex;
		align-items: center;
		justify-content: center;
		
		padding: 0rem 1rem;
	}
	div {
		display: flex;
		flex-direction: column;
		align-items: center;

		h1 {
			padding: 0rem 1rem;
			color: white;
			font-size: clamp(4em, 15vw, 6em);
			text-shadow: #ff0000 0px 0 35px;
			-webkit-text-stroke-width: 3px;
			-webkit-text-stroke-color: black;
		}
		img{
			margin-top: clamp(3rem, 15vw, 5rem);
			width: 30rem;
			width: clamp(15rem, 40vw, 30rem);
			animation: breathing 2s infinite ease-in-out;
		}
	}
	@keyframes breathing {
    0%, 100% {
        transform: scale(1);
        filter: drop-shadow(0px 0px 20px #ff0000);
    }
    50% {
        transform: scale(1.05);
        filter: drop-shadow(0px 0px 15px #ff0000);
    }
}

@media (width <= 400px) {
    main {
      padding: 0 1rem;
    }
  }
</style>
